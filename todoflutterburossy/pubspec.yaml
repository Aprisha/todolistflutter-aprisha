name: todo_flutter_app
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1


  # Tambahkan dependensi yang diperlukan
  http: ^0.13.6
  shared_preferences: ^2.2.2

  # Jika kamu pakai state management atau lainnya, contoh:
  provider: ^6.1.2

  # Flutter core utilities
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
